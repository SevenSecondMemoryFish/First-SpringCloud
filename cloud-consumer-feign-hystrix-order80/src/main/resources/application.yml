server:
  port: 80
spring:
  application:
    name: cloud-feign-hystrix-order-server

eureka:
  client:
    # 注册进入eureka
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka   # 单机版

feign:
  hystrix:
    enabled: true
