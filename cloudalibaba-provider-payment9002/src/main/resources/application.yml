server:
  port: 9002
spring:
  application:
    name: provider-payment
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

# 暴露端口
management:
  endpoints:
    web:
      exposure:
        include: "*"